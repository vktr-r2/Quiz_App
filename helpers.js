// results page.

// we want the server to obtain results from the data base.

// if response is correct, push to is_right array.

// if response is wrong, push to is_wrong array.

// calculate % of is_right using length of array.

// if is right >= 50% render div in ejs page says Great Job.

// if is right < 50% render div in results.ejs which says Not SO great!

// provide link to share results with friends.

// page displays all the questions in the quiz and their correct answers
